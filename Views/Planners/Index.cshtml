@model IEnumerable<SacramentMeetingPlanner.Models.Planner>

@{
    ViewData["Title"] = "Planner";
}

<h1>Planner</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.President)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conducting)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invocation)
            </th>  
            <th>
                @Html.DisplayNameFor(model => model.Benediction)
            </th>            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.President)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conducting)
            </td>  
            <td>
                @Html.DisplayFor(modelItem => item.Invocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Benediction)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlannerID" class="btn btn-primary">Edit</a> 
                <a asp-action="Details" asp-route-id="@item.PlannerID" class="btn btn-warning">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.PlannerID" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
