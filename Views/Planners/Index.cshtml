@model IEnumerable<SacramentMeetingPlanner.Models.Planner>

@{
    ViewData["Title"] = "Planner";
}

<h1>Planner</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Member1ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Speaker4ID)
            </th>            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member1ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Speaker.Member.FullName)
            </td>            
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlannerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlannerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlannerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
